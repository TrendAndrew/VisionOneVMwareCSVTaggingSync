/**
 * Vision One custom tag domain model.
 *
 * Custom tags are user-defined labels that can be applied
 * to endpoints for grouping and policy assignment.
 */

export interface VisionOneCustomTag {
  tagId: string;
  tagName: string;
}
